Algoritmo "somar_parametro_funcao"

Var
   v1, v2, s: inteiro

Funcao soma(x, y: inteiro): inteiro
inicio
      Retorne x + y
FimFuncao

Inicio
    escreva("Digite o primeiro valor: ")
    leia(v1)
    escreva("Digite o segundo valor: ")
    leia(v2)
    s <- soma(v1, v2)
    escreval("A soma entre ", v1, " e ", v2, " é igual a ", s)

Fimalgoritmo
